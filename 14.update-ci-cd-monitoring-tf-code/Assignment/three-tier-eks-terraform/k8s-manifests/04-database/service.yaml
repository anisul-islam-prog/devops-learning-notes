apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: three-tier
spec:
  type: ClusterIP
  selector:
    app: database
    tier: db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres